function fnlogin(elemento) {
    // console.log("elemento clickeado", elemento);
    if(elemento.innerText == "Login"){
        elemento.innerText="Logout"
    }else{
        elemento.innerText="Login"
    }
   
}

